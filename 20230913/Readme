2023년 9월 13일에는 JSP에서 사용하는 커스텀 태그에 대해 배웠다.

오늘 배운 커스텀 태그는 기본적으로 제공되는 태그인 jsp태그를 통해, 페이지 안에 페이지를 출력시키는 include 방식의 페이지 이동에 사용할 수 있었다.
또한, 외부에서 제공받은 라이브러리인 jstl.jar를 통한 외부 태그 'core' 를 통해 jsp에서 DOM객체 양식으로 조건문, 반복문, 예외처리문(try-catch)을 사용할 수 있었다.
또한, jstl.jar의 'fmt' 를 통해 DOM객체 양식으로 문자열, 숫자, 날짜 형태를 포맷팅(Format)할 수 있었다.

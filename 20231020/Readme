10월 20일에는, 파이썬에서의 JSON 데이터 파싱과 파이썬에서 OracleDB에 접근하여 CRUD(Create, Read, Update, Delete)를 진행해 보았다.

파이썬의 경우 Java보다도 파싱에 필요한 코드가 적어 간편하다. API에 연결하는 방법에만 익숙해진다면, 보다 간단하게 데이터 파싱이 가능할 것 같았다.

DB의 경우에도, CRUD 모두 비슷한 양식의 코드로 진행되며, SQL문만 잘 작성하면 파이썬 코드 상에서는 큰 어려움이 없었던 것 같다.
